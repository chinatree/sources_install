#!/bin/bash

# Configuration python ext of function
configuration_python_ext_setuptools()
{
    local module="Python"
    local version="${python_ext_setuptools_version}"

    ln -sf "${install_dir_prefix}/${module}/bin/easy_install" "${path_local_bin}"
}
